# CREATE TABLE IF NOT EXISTS finance_db.finance_data (
#                                                        id BIGINT AUTO_INCREMENT PRIMARY KEY,
#                                                        base_currency VARCHAR(10) NOT NULL,
#                                                        target_currency VARCHAR(10) NOT NULL,
#                                                        exchange_rate DOUBLE NOT NULL
# );